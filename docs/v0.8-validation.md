# Seer v0.8 validation

**Status: v0.8 complete within the validation scope below; Claude host tool use remains unverified.** No release or tag has been published. Native MCP and Codex workflows passed within the measured scope. Claude Code could not authenticate; its model-backed evaluation is deferred and is not a completion requirement for v0.8.

Measured on macOS 27.0 (Apple Silicon). The base CLI used Python 3.9.6 and Pillow 11.3.0; the isolated MCP environment used Python 3.13.12, Pillow 12.3.0, and MCP SDK 2.2.0. Python 3.10 is the SDK's installation minimum, not an environment exercised by this run.

## Completed checks

| Check | Result |
|---|---|
| Base CLI suite | 82 tests passed, 5 optional MCP tests skipped; 8.447 seconds. |
| SDK environment suite | All 87 tests passed in 14.312 seconds, with ResourceWarnings treated as errors. |
| MCP stdio | Legacy initialization and modern request metadata exercised; tool help, CLI verdicts, saved-report parity, evidence paths, and canonical argument errors checked. |
| Baseline policy | Creation and replacement rejected; the existing synthetic baseline bytes remained unchanged. Abbreviated CLI flags were rejected before execution. |
| Deadline/cancellation | CLI timeout, asyncio cancellation, AnyIO cancellation with a SIGTERM-resistant child, and cleanup of separate native/stability process groups passed. |
| Existing utilities | 5 Excalidraw tests, all shell syntax checks, relevant ShellCheck, and strict Claude plugin validation passed. |

Commands used:

```bash
python3 -m unittest discover -s tests -v
.seer/qa/v08-venv/bin/python -W error::ResourceWarning -m unittest discover -s tests -v
python3 skills/seer/scripts/test_excalidraw.py
for script in skills/seer/scripts/*.sh; do bash -n "$script"; done
shellcheck skills/seer/scripts/loop_compare.sh skills/seer/scripts/capture_app_window.sh
claude plugin validate --strict .
```

Local suite logs are `.seer/qa/v08-base-unittest.log` and `.seer/qa/v08-sdk-unittest.log`. CI installs the optional requirements before running the same suite. These tests use synthetic images and process substitutes; they do not establish arbitrary app compatibility.

## Native MCP fixture

Seven checks passed through a real stdio MCP connection: window discovery, exact-window capture, matching AX text, a valid text mismatch, semantic wait, a missing baseline, and a closed-window error. The missing baseline was not created. The fresh 1040 × 784 PNG was inspected visually and its capture hash verified; it shows the fixture's labels and controls. Capture took 0.424 seconds, the first AX assertion 1.280 seconds, and semantic wait 0.282 seconds in this single run. These are observations, not performance guarantees.

The report and raw capture remain local at `.seer/qa/v08-native-gmrufds6/report.json` and its adjacent `.seer/capture/` directory. No user's existing app content or privacy settings were changed.

## Host workflow evaluation

The same representative task asks each host to call `seer_help` for `verify`, then compare a synthetic matching image, a changed image, and an image with a missing baseline. The fixture baseline was prepared only inside the isolated test directory.

| Host | Observed result |
|---|---|
| Codex CLI 0.149.1 | Four MCP calls completed; it correctly reported `pass`, `fail`, and `needs_baseline`. The missing baseline remained absent. The invocation took 17.896 seconds. |
| Claude Code 2.1.218 | Temporary MCP configuration was accepted, but model execution stopped because the local OAuth session could not refresh. A subsequent login attempt could not authorize subscription access. No Claude tool calls completed; model-backed behavior remains unverified. |

Codex used per-run configuration overrides with `--ignore-user-config`, `--ephemeral`, and an explicit project root. Claude used `--mcp-config`, `--strict-mcp-config`, no session persistence, and only the two Seer tools allowed. Saved host configurations were not changed. Evidence is local under `.seer/qa/v08-hosts-fn88x867/`, including `host-report.json` and each host's event log.

The Claude example was corrected to place another option after the variadic `--mcp-config` flag so the prompt cannot be consumed as a configuration filename. No repeated workflow confusion was observed in the completed Codex task; the Seer skill remains intact. Claude's behavior cannot yet be assessed. A future Claude evaluation requires eligible authenticated access and must confirm all three statuses and baseline preservation before Claude compatibility is claimed. Passing SDK, native fixture, and Codex checks does not establish Claude model-backed behavior.

## Protocol references and reproduction

The adapter uses the official Python SDK 2.2.0 rather than implementing MCP framing or negotiation itself. The reference review covered the [2026-07-28 versioning rules](https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning), [stdio transport](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports), [tool result contract](https://modelcontextprotocol.io/specification/2026-07-28/server/tools), and [Python SDK low-level server](https://py.sdk.modelcontextprotocol.io/v2/advanced/low-level-server/). Host configuration references are linked from the [integration guide](mcp-integration.md).

The native fixture check is reproducible with an isolated Python environment containing `skills/seer/scripts/requirements-mcp.txt`:

```bash
xcrun swiftc tests/manual/window_fixture.swift -o .seer/qa/SeerWindowFixture
.local/seer-mcp-venv/bin/python tests/manual/verify_v08.py
```

The fixture runner controls only its own AppKit windows, saves local results under `.seer/qa/v08-native-*`, and does not create a real baseline. Inspect the returned fresh preview before claiming visual validation. Prior semantic/OCR coverage remains in [v0.7 validation](v0.7-validation.md); it is not automatically expanded by the adapter.
